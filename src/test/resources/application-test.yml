spring:
  config:
    activate:
      on-profile: test
  jpa:
    database: h2
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:dbtest
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
